const allowedRootDomain = "igo-bokep.icu";
const currentHost = window.location.host;

if (!currentHost.endsWith(allowedRootDomain)) {
  window.location.href = "https://tv1.igo-bokep.icu/?utm_source=redirect_auto";
}
